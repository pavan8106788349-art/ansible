  - name: install packages
    hosts: local
    connections: local
    become: Yes
    tasks:
    - name: install package
      ansible.builtin.package:
          name: "{{ item}}"
          state: present
      loop:
        - mysql
        - nginx
        - zip  